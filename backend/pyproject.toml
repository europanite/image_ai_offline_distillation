[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = ["build", "dist", "node_modules", ".venv", "reports"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "RUF"]
ignore = ["B008"]
